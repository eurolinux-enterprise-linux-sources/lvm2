 WHATS_NEW         | 5 +++++
 lib/metadata/lv.c | 4 +++-
 tools/lvchange.c  | 2 +-
 3 files changed, 9 insertions(+), 2 deletions(-)

diff --git a/WHATS_NEW b/WHATS_NEW
index cc40991..7a3f9ab 100644
--- a/WHATS_NEW
+++ b/WHATS_NEW
@@ -2,6 +2,11 @@ Version 2.02.167 -
 =================================
   Prevent non-synced raid1 repair unless --force
 
+Version 2.02.166 - 
+=====================================
+  Fix lvchange --discard|--zero for active thin-pool.
+  Fix lock-holder device for thin pool with inactive thin volumes.
+
 Version 2.02.162 - 
 =================================
   Fix automatic updates of PV extension headers to newest version.
diff --git a/lib/metadata/lv.c b/lib/metadata/lv.c
index 98b1ddb..17bf67c 100644
--- a/lib/metadata/lv.c
+++ b/lib/metadata/lv.c
@@ -1428,13 +1428,15 @@ const struct logical_volume *lv_lock_holder(const struct logical_volume *lv)
 	if (lv_is_cow(lv))
 		return lv_lock_holder(origin_from_cow(lv));
 
-	if (lv_is_thin_pool(lv))
+	if (lv_is_thin_pool(lv)) {
 		/* Find any active LV from the pool */
 		dm_list_iterate_items(sl, &lv->segs_using_this_lv)
 			if (lv_is_active(sl->seg->lv)) {
 				log_debug("Thin volume \"%s\" is active.", sl->seg->lv->name);
 				return sl->seg->lv;
 			}
+		return lv;
+	}
 
 	/* RAID changes visibility of splitted LVs but references them still as leg/meta */
 	if ((lv_is_raid_image(lv) || lv_is_raid_metadata(lv)) && lv_is_visible(lv))
diff --git a/tools/lvchange.c b/tools/lvchange.c
index 316a786..3e4ae12 100644
--- a/tools/lvchange.c
+++ b/tools/lvchange.c
@@ -137,7 +137,7 @@ static int _lvchange_pool_update(struct cmd_context *cmd,
 	if (!update)
 		return 0;
 
-	if (!lv_update_and_reload_origin(lv))
+	if (!lv_update_and_reload(lv))
 		return_0;
 
 	return 1;
